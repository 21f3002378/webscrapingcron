name: 21f3002378@ds.study.iitm.ac.in

on:
  schedule:
    - cron: "17 2 * * *"  # Runs every day at 02:30 UTC

  workflow_dispatch:  # Allows manual trigger from GitHub Actions UI

jobs:
  daily-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Git with email 21f3002378@ds.study.iitm.ac.in 
        run: |
          echo "hello"

      - name: Append "Hello" to log.txt
        run: echo "Hello" >> log.txt

      - name: Commit and push changes
        run: |
          git add log.txt
          git commit -m "Automated: Added Hello"
          git pull --rebase
          git push origin main
          
